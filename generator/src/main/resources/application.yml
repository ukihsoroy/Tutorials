server.port: 1337

spring:
  application.name: ko-generator
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    ip: 111.231.224.68
    port: 3306
    name: art-prototype
    username: root
    password: tiger
    url: jdbc:mysql://${spring.datasource.ip}:${spring.datasource.port}/${spring.datasource.name}?autoReconnect=true&amp;useUnicode=true&amp;characterEncoding=utf-8&amp;useSSL=false

generator:
  moduleName: case-generator
  tables: art_link, art_region
  enable: false   #开启后生成全库

rootPackage: org.ko.generator
domainPackage: entity #数据库域对象包名
mapperPackage: dao
xmlPackage: mappers

java.enable: true #开启后生成后台代码

view:
  filePath: D://
  enable: true    #开启后生成前台代码
  js:
  html:

copyProject:
  name: project-name
  path: D://
  enable: false
